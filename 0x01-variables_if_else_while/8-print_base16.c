#include <stdio.h>

/**
 * main - the program entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char ch;
	int num;

	for (num = 48; num <= 57; num++)
	{
		putchar(n);
	}
	for (ch = 'a'; ch <= 'f'; ch++)
	{
		putchar(ch);
	}
	putchar(10);
	return (0);

}
