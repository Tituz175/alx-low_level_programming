#include <stdio.h>
#include "main.h"

/**
 * mul - this function compute multiplication of two
 * given numbers
 * @a: the first given number
 * @b: the second given number
 * Return: this will the output of the multiplication
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
