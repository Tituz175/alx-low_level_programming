#include "main.h"

/**
 * mul - function that multiply two int.
 * @a: first int number
 * @b: second int number
 * Return: 1 if c is digit between 0 and 9, 0 otherwise
 */

int mul(int a, int b)
{
	return (a * b);
}

